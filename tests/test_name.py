import pytest

from masterdata_checker.name import name_checker


# @pytest.mark.parametrize(
#     'file_path, result',
#     [
#         ('whatever-filepath', ['whatever-result-we-expert', True]),
#         ('another-filepath', ['another-result-we-expert', False]),
#     ]
# )
# def test_name_checker(filepath: str, result: list[str, bool]):
def test_name_checker():
    """
    Tests the `name_checker` function defined in the `name.py` module.
    """

    # result = name_checker(file_path=file_path)

    assert True  # dummy test as a placeholder
